# Shared UI Components

This repository currently contains static HTML pages (`index.html`, `index1.html`) and does not yet have a dedicated reusable component directory (such as `src/components/ui`).

## Current status
- Shared primitive components (Button/Input/Card/etc.) as standalone files: **Not present**
- Component library framework: **Not present**

## Next extraction target
From `index1.html`, the following patterns are good candidates to extract when the codebase is migrated to React/Vue:
- `NavPillMenu`
- `GlassCard`
- `SkillCard`
- `ProjectCard`
- `ContactPanel`
